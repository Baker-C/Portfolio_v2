export const colors = {
  black: '#000000',
  white: '#ffffff',
  /** Near-white pink accent — fluid dots, borders, and tinted shadows */
  theme: '#e8ffed',
} as const;

export type Colors = typeof colors;
